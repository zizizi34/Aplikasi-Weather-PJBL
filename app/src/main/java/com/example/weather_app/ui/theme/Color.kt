package com.example.weather_app.ui.theme

import androidx.compose.ui.graphics.Color

// Primary palette (blue)
val Primary = Color(0xFF1E88E5)        // Blue 600
val PrimaryContainer = Color(0xFF90CAF9) // Light blue for dark theme accents
val OnPrimary = Color(0xFFFFFFFF)

// Secondary palette (teal)
val Secondary = Color(0xFF26A69A)      // Teal 400
val OnSecondary = Color(0xFFFFFFFF)

// Surface / background
val Background = Color(0xFFF1F7FF)     // Very light blue background
val OnBackground = Color(0xFF0B1F3A)   // Dark navy text on background
val Surface = Color(0xFFFFFFFF)
val OnSurface = Color(0xFF0B1F3A)

// Error
val Error = Color(0xFFB00020)
val OnError = Color(0xFFFFFFFF)

// Helpful dark theme anchors
val DarkBackground = Color(0xFF071427) // Deep navy for dark backgrounds
val DarkSurface = Color(0xFF0F283C)    // Slightly lighter surface in dark mode
val OnDark = Color(0xFFFFFFFF)
